import 'package:flutter/material.dart';

class AboutWynk extends StatelessWidget {
  const AboutWynk({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
